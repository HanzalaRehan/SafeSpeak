# SafeSpeak — minimal requirements for macOS (Apple Silicon / M4)
# Install with:  pip install -r requirements.txt

# Core vision
opencv-python>=4.8
mediapipe>=0.10          # Face Mesh; ARM wheels available via PyPI

# VSR model dependencies
torch>=2.2               # MPS (Metal) support for Apple Silicon
torchvision>=0.17
transformers>=4.40       # HuggingFace pipeline
huggingface-hub>=0.22    # model weight downloads
Pillow>=10.0

# ONNX fallback (optional but recommended)
onnxruntime>=1.18        # CoreML execution provider on macOS

# File watching (Worker 3)
watchdog>=4.0

# Utilities
numpy>=1.26
